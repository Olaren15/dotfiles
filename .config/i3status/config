general {
  colors = true
  color_separator = "#458588"
  color_good = "#98971a"
  color_degraded = "#d79921"
  color_bad = "#cc241d"

  output_format = "i3bar"

  interval = 5
}

order += "volume master"
order += "time"

time {
  format = "%Y-%m-%d %H:%M"
}

volume master {
  format = "  %volume"
  format_muted = "  %volume (muted)"
  device = "default"
}
